apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: confirm-payment-data
  name: confirm-payment-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
