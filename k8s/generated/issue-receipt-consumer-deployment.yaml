apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml -o k8s/generated/
    kompose.version: 1.38.0 (a8f5d1cbd)
  labels:
    io.kompose.service: issue-receipt-consumer
  name: issue-receipt-consumer
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: issue-receipt-consumer
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yml -o k8s/generated/
        kompose.version: 1.38.0 (a8f5d1cbd)
      labels:
        io.kompose.service: issue-receipt-consumer
    spec:
      containers:
        - args:
            - python
            - consumer.py
          env:
            - name: DATABASE_PATH
              value: /data/issue_receipt.db
            - name: LOG_LEVEL
              value: INFO
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://otel-collector:4318
            - name: OTEL_SERVICE_NAME
              value: issue-receipt-consumer
            - name: RABBITMQ_URL
              value: amqp://challenge:challenge@rabbitmq:5672/%2F
          image: issue-receipt-consumer:latest
          imagePullPolicy: IfNotPresent
          name: issue-receipt-consumer
          volumeMounts:
            - mountPath: /data
              name: issue-receipt-data
      restartPolicy: Always
      volumes:
        - name: issue-receipt-data
          persistentVolumeClaim:
            claimName: issue-receipt-data
