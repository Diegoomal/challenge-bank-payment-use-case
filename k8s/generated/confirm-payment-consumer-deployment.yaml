apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml -o k8s/generated/
    kompose.version: 1.38.0 (a8f5d1cbd)
  labels:
    io.kompose.service: confirm-payment-consumer
  name: confirm-payment-consumer
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: confirm-payment-consumer
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yml -o k8s/generated/
        kompose.version: 1.38.0 (a8f5d1cbd)
      labels:
        io.kompose.service: confirm-payment-consumer
    spec:
      containers:
        - args:
            - python
            - consumer.py
          env:
            - name: DATABASE_PATH
              value: /data/confirm_payment.db
            - name: LOG_LEVEL
              value: INFO
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://otel-collector:4318
            - name: OTEL_SERVICE_NAME
              value: confirm-payment-consumer
            - name: RABBITMQ_URL
              value: amqp://challenge:challenge@rabbitmq:5672/%2F
          image: confirm-payment-consumer:latest
          imagePullPolicy: IfNotPresent
          name: confirm-payment-consumer
          volumeMounts:
            - mountPath: /data
              name: confirm-payment-data
      restartPolicy: Always
      volumes:
        - name: confirm-payment-data
          persistentVolumeClaim:
            claimName: confirm-payment-data
