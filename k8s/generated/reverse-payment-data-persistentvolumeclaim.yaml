apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: reverse-payment-data
  name: reverse-payment-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
