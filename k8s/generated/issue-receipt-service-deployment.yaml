apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml -o k8s/generated/
    kompose.version: 1.38.0 (a8f5d1cbd)
  labels:
    io.kompose.service: issue-receipt-service
  name: issue-receipt-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: issue-receipt-service
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yml -o k8s/generated/
        kompose.version: 1.38.0 (a8f5d1cbd)
      labels:
        io.kompose.service: issue-receipt-service
    spec:
      containers:
        - env:
            - name: DATABASE_PATH
              value: /data/issue_receipt.db
            - name: LOG_LEVEL
              value: INFO
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://otel-collector:4318
            - name: OTEL_SERVICE_NAME
              value: issue-receipt-service
            - name: RABBITMQ_URL
              value: amqp://challenge:challenge@rabbitmq:5672/%2F
          image: issue-receipt-service:latest
          imagePullPolicy: IfNotPresent
          name: issue-receipt-service
          ports:
            - containerPort: 8000
              protocol: TCP
          volumeMounts:
            - mountPath: /data
              name: issue-receipt-data
      restartPolicy: Always
      volumes:
        - name: issue-receipt-data
          persistentVolumeClaim:
            claimName: issue-receipt-data
