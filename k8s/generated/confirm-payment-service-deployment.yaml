apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml -o k8s/generated/
    kompose.version: 1.38.0 (a8f5d1cbd)
  labels:
    io.kompose.service: confirm-payment-service
  name: confirm-payment-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: confirm-payment-service
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yml -o k8s/generated/
        kompose.version: 1.38.0 (a8f5d1cbd)
      labels:
        io.kompose.service: confirm-payment-service
    spec:
      containers:
        - env:
            - name: DATABASE_PATH
              value: /data/confirm_payment.db
            - name: LOG_LEVEL
              value: INFO
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://otel-collector:4318
            - name: OTEL_SERVICE_NAME
              value: confirm-payment-service
            - name: RABBITMQ_URL
              value: amqp://challenge:challenge@rabbitmq:5672/%2F
          image: confirm-payment-service:latest
          imagePullPolicy: IfNotPresent
          name: confirm-payment-service
          ports:
            - containerPort: 8003
              protocol: TCP
          volumeMounts:
            - mountPath: /data
              name: confirm-payment-data
      restartPolicy: Always
      volumes:
        - name: confirm-payment-data
          persistentVolumeClaim:
            claimName: confirm-payment-data
