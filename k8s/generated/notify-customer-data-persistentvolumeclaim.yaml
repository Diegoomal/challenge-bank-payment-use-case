apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: notify-customer-data
  name: notify-customer-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
