apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: debit-account-data
  name: debit-account-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
