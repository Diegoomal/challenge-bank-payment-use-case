apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: issue-receipt-data
  name: issue-receipt-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
